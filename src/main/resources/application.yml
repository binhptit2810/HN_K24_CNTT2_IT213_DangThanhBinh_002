spring:
  application:
    name: Hackathon_De01
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DATABASE_HOST:aws-0-ap-northeast-1.pooler.supabase.com}:${DATABASE_PORT:6543}/${DATABASE_NAME:postgres}?sslmode=require&prepareThreshold=0
    username: ${DATABASE_USERNAME:postgres.gzdleoedskonlspqzjmw}
    password: Binh28102006@111
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY:}
        chat:
          options:
            model: gemini-2.5-flash
    openai:
      base-url: https://openrouter.ai/api/v1
      api-key: ${OPENAI_API_KEY:}
      chat:
        model: anthropic/claude-3-haiku
      embedding:
        model: text-embedding-3-small
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1536
        initialize-schema: true
